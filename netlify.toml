[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[dev]
  command = "vite"
  port = 8888
  targetPort = 5173
  framework = "#custom"

[functions]
  node_bundler = "esbuild"
  included_files = ["src/lib/pricing.ts"]

[functions."pdf-quote"]
  memory_size = 1024
  external_node_modules = ["pdfkit"]

[functions."pdf-subscription"]
  memory_size = 1024
  external_node_modules = ["pdfkit"]

[functions."agreements"]
  memory_size = 1024
  external_node_modules = ["pdfkit"]

[functions."subcontractor-agreements"]
  memory_size = 1024
  external_node_modules = ["pdfkit", "resend"]

[functions."esign"]
  external_node_modules = ["resend"]

[functions."quotes"]
  external_node_modules = ["resend"]

[functions."agent"]
  timeout = 60
  included_files = ["src/lib/pricing.ts"]

[functions."send-reminders"]
  schedule = "0 14 * * *"

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
